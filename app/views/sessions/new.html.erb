<div class="block" id="block-login">
  <h2>Zaloguj się</h2>
  <div class="content login">
    <div class="flash">
      <% flash.each do |type, message| %>
        <div class="message notice">
          <p>
            <%= message %>
          </p>
        </div>
      <% end %>
    </div>
  <%= form_tag do %>
    <fieldset>
      <legend>Panel logowania</legend>
      <div class="group wat-cf">
        <div class="left">
          <%= label_tag :name, 'Nazwa:' %>
        </div>
        <div class="right">
        <%= text_field_tag :name, params[:name] %>
        </div>
      </div>
      <div class="group wat-cf">
        <div class="left"><%= label_tag :password, 'Hasło:' %></div>
        <div class="right"><%= password_field_tag :password, params[:password] %></div>
      </div>
      <div class="group wat-cf">
        <div class="right"><div class="button"><%= submit_tag "Zaloguj" %></div></div>
      </div>
    </fieldset>
  <% end %>
  </div>
</div>